<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Teamer.APP.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <Grid Padding="20,0,20,0" VerticalOptions="Center">
            <Entry Text="{Binding LoginName}}" x:Name="LoginEntry" Placeholder="Name:" PlaceholderColor="Gray" Grid.Row="2"/>
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}" Grid.Row="10"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>